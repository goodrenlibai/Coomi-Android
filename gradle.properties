org.gradle.jvmargs=-Xmx4096M
android.useAndroidX=true

minSdkVersion=24
# Termux executes its bootstrap from the app-private files directory. Android
# blocks that execution model for apps targeting API 29+, so this must remain 28
# until the runtime is moved into read-only APK native libraries.
targetSdkVersion=28
ndkVersion=27.0.12077973
compileSdkVersion=36

markwonVersion=4.6.2

android.nonTransitiveRClass=true
